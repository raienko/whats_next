import React from 'react';
import Router from 'src/screens/Router';
import Preferences from './Preferences';

const App = () => {
  return (
    <Preferences>
      <Router />
    </Preferences>
  );
};

export default App;
