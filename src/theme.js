import {rem} from './utils/metrics';

export const height = rem(50);

export const borderRadius = rem(5);

export const offset = rem(10);

export const borderWidth = rem(1);

export const colors = {
  basic: '#fff',
  inverted: '#000',
  primary: '#FFD81E',
  secondary: '#7A1CAD',
  tertiary: '#244EAE',
  disabled: '#999',
};

export const shadow = {
  shadowOffset: {
    width: 0,
    height: 2,
  },
  shadowOpacity: 0.3,
  shadowRadius: 2,
  elevation: 4,
};
